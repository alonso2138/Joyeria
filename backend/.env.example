# Backend Environment Variables

# Puerto del servidor
PORT=5000

# Entorno de ejecución
NODE_ENV=development

# MongoDB Connection String
# Formato: mongodb+srv://usuario:password@cluster.mongodb.net/nombre_db?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/joyas?retryWrites=true&w=majority

# JWT Secret para autenticación de admin
# Generar uno aleatorio con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_here

# URL del frontend (para CORS)
FRONTEND_URL=http://localhost:5173
